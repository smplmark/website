/// <reference types="@cloudflare/vitest-pool-workers/types" />

// Provides the `cloudflare:test` module types (SELF, etc.) for the Worker tests. The website Worker
// has no test-only bindings — just static assets — so there is nothing to augment here.
